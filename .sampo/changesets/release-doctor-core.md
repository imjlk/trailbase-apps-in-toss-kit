---
npm/@trailbase-apps-in-toss-kit/trailbase-runtime: patch
---

Add a reusable release doctor for preQA and production handoff checks. The runtime package now exports release-doctor helpers and a CLI that can combine production env validation, app-owned commands, and optional Sampo changeset checks into one normalized result.
