---
npm/@trailbase-apps-in-toss-kit/trailbase-client: patch
---

Add an Apps in Toss Storage-backed `KeyValueStorage` adapter for React Native and WebView mini-apps.
Consumers can inject the official `Storage` bridge for production session persistence while keeping
memory or localStorage fallbacks limited to local tests.
