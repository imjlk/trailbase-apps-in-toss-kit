---
cargo/trailbase-guest-common: minor
---

Add shared promotion reward helpers for fixed and capped grant amounts, generic provider payloads,
provider outcome normalization, amount-aware campaign availability checks, and adapter-based reward
usage queries.
