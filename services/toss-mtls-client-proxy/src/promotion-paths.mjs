import { PROXY_ENDPOINTS } from "@ait-kit/api-client";

// Canonical status path, shared with api-client's proxy endpoint map.
export const PROMOTION_REWARD_STATUS_PATH = PROXY_ENDPOINTS.promotionRewardStatus;
