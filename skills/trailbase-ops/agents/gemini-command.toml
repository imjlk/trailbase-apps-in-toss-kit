description = "Apply TrailBase AppsInToss migration, RN bootstrap, WASM, deployment, and mTLS proxy guardrails."

prompt = """
Use trailbase-ops for this task. Treat skills/trailbase-ops/SKILL.md as the
canonical source and read skills/trailbase-ops/references/trailbase-ops.md when
the work touches TrailBase migrations, auth principal mapping, React Native
client bootstrap, Record API ACL, Rust WASM guests, Coolify deployment,
fresh-start behavior, or mTLS proxy settings.

Keep production-like data durable by default. Prefer additive, forward-only
migrations; do not rewrite baseline SQL unless the user explicitly requests a
reset. Keep AppsInToss anonymous users mapped to TrailBase _user through the
official auth flow, enforce disabled app auth states in custom endpoints, verify
config.textproto when Record API exposure changes, prefer existing kit client
adapter helpers before app-local SDK/storage/login/session wrappers, run the
repo's wasm32-wasip2 check after WASM edits, and keep mTLS certificates mounted
only into the proxy service.
"""
