interface:
  display_name: "TrailBase Ops"
  short_description: "TrailBase auth, migration, and ops guardrails"
  default_prompt: "Use $trailbase-ops to plan a safe TrailBase auth, migration, WASM, production check, or mTLS proxy operation."
